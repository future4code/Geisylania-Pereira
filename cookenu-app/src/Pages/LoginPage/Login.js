import React from "react";
import { Button } from "@material-ui/core";

const LoginPage = () => {
    return(
        <div>
            <h1>Login</h1>
            <Button variant="contained">Concluir Login</Button>
        </div>
    )
}

export default LoginPage